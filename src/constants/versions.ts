// The exact react-ui-animate npm versions live CodeExample sandboxes pin to.
// Keep these in sync with versions.json / the `next` dist-tag.
export const STABLE_VERSION = '6.0.0';
export const NEXT_VERSION = '6.0.0';
