---
sidebar_position: 2
id: useGesture
title: useGesture
---

import CodeExample from '@site/src/components/CodeExample';
import Code_gesture_useGesture_PanExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/PanExample.tsx';
import Demo_gesture_useGesture_PanExample from '@site/src/examples/next/gesture/useGesture/PanExample';
import Code_gesture_useGesture_MoveExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/MoveExample.tsx';
import Demo_gesture_useGesture_MoveExample from '@site/src/examples/next/gesture/useGesture/MoveExample';
import Code_gesture_useGesture_WheelExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/WheelExample.tsx';
import Demo_gesture_useGesture_WheelExample from '@site/src/examples/next/gesture/useGesture/WheelExample';
import Code_gesture_useGesture_ScrollExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/ScrollExample.tsx';
import Demo_gesture_useGesture_ScrollExample from '@site/src/examples/next/gesture/useGesture/ScrollExample';
import Code_gesture_useGesture_SwipeExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/SwipeExample.tsx';
import Demo_gesture_useGesture_SwipeExample from '@site/src/examples/next/gesture/useGesture/SwipeExample';
import Code_gesture_useGesture_HoverExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/HoverExample.tsx';
import Demo_gesture_useGesture_HoverExample from '@site/src/examples/next/gesture/useGesture/HoverExample';
import Code_gesture_useGesture_PinchExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/PinchExample.tsx';
import Demo_gesture_useGesture_PinchExample from '@site/src/examples/next/gesture/useGesture/PinchExample';
import Code_gesture_useGesture_RotateExample from '!!raw-loader!@site/src/examples/next/gesture/useGesture/RotateExample.tsx';
import Demo_gesture_useGesture_RotateExample from '@site/src/examples/next/gesture/useGesture/RotateExample';

`useGesture` attaches a `Gesture.*` descriptor to an element, `window`, or an
array of elements. Config and handlers stay live across renders, so editing
them after mount takes effect without re-attaching listeners.

```ts
useGesture(ref, descriptor);
useGesture(window, descriptor);
useGesture(refs, descriptor | ((index: number) => descriptor));
```

Every `Gesture.*()` call returns a chainable builder. Call `.onStart()`,
`.onChange()`, `.onEnd()` (and, where relevant, `.onFinalize()` or
`.onSwipe()`) plus any config methods, then pass the built descriptor to
`useGesture`.

## Gesture.Pan()

Streams a full press-move-release cycle. `useDrag` is built on this. Reach
for `Gesture.Pan()` directly when you need drag data without `useDrag`'s
built-in spring/decay behavior.

```jsx
import { useRef } from 'react';
import { Gesture, useGesture, useValue, animate } from 'react-ui-animate';

function Panner() {
  const ref = useRef(null);
  const [x, setX] = useValue(0);

  // highlight-start
  /* Stream pan movement into an animated value */
  useGesture(
    ref,
    Gesture.Pan()
      .onChange(({ down, movement }) => setX(movement.x))
      .onEnd(() => setX(0))
  );
  // highlight-end

  return <animate.div ref={ref} style={{ translateX: x }} />;
}
```

Config: `.enabled(bool)`, `.minDistance(px)`, `.axis('x' | 'y')`.
Event shape: `{ phase, down, movement, offset, velocity, event, target, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_PanExample}
  code={Code_gesture_useGesture_PanExample}
/>

## Gesture.Move()

Tracks pointer position with no press required: cursor-follow effects,
magnetic hover.

```jsx
useGesture(
  window,
  Gesture.Move().onChange(({ offset }) => setCursor(offset))
);
```

Event shape: `{ movement, offset, velocity, event, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_MoveExample}
  code={Code_gesture_useGesture_MoveExample}
/>

## Gesture.Wheel()

Trackpad/mouse-wheel deltas, for custom zoom or horizontal scroll.

```jsx
useGesture(
  ref,
  Gesture.Wheel().onChange(({ movement }) => setZoom((z) => z - movement.y * 0.001))
);
```

Event shape: `{ movement, offset, velocity, event, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_WheelExample}
  code={Code_gesture_useGesture_WheelExample}
/>

## Gesture.Scroll()

Native scroll events with movement/velocity, for scroll-linked animation and
progress bars. (For intersection-based reveal-on-scroll, see
[useInView](../hooks/useInView) or the [`view`](../interactive/view-animations) prop
instead: `Gesture.Scroll()` is for continuous scroll position, not
enter/exit detection.)

```jsx
useGesture(
  window,
  Gesture.Scroll().onChange(({ offset }) => {
    const max = document.documentElement.scrollHeight - window.innerHeight;
    setProgress(offset.y / max);
  })
);
```

Event shape: `{ movement, offset, velocity, event, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_ScrollExample}
  code={Code_gesture_useGesture_ScrollExample}
/>

## Gesture.Swipe()

A fast, short, directional flick resolved once at release. Not a stream:
only `onSwipe` fires, and only if the release clears both thresholds.

```jsx
useGesture(
  ref,
  Gesture.Swipe()
    .distanceThreshold(40)
    .onSwipe(({ direction }) => {
      if (direction === 'left') goNext();
      if (direction === 'right') goBack();
    })
);
```

Config: `.enabled(bool)`, `.axis('x' | 'y')`, `.velocityThreshold(pxPerMs)`
(default `0.5`), `.distanceThreshold(px)` (default `30`).
Event shape: `{ direction, movement, velocity, event, target }`.

<CodeExample
  demo={Demo_gesture_useGesture_SwipeExample}
  code={Code_gesture_useGesture_SwipeExample}
/>

## Gesture.Hover()

Enter/leave/move-while-over: deliberately leaner than `Gesture.Move()` (no
movement/velocity). Use `Gesture.Move()` instead if you need those while
hovering.

```jsx
useGesture(
  ref,
  Gesture.Hover().onChange(({ hovering }) => setIsHovering(hovering))
);
```

Event shape: `{ hovering, offset, event, target }`.

<CodeExample
  demo={Demo_gesture_useGesture_HoverExample}
  code={Code_gesture_useGesture_HoverExample}
/>

## Gesture.Pinch()

Two-finger pinch-to-zoom. `scale` is the current pointer-pair distance
relative to where the gesture began.

```jsx
useGesture(
  ref,
  Gesture.Pinch().onChange(({ scale }) => setZoom(scale))
);
```

Config: `.enabled(bool)`, `.threshold(v)` (minimum `|scale - 1|` before
recognized as active, default `0.02`).
Event shape: `{ phase, scale, velocity, center, event, target, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_PinchExample}
  code={Code_gesture_useGesture_PinchExample}
/>

## Gesture.Rotate()

Two-finger rotation, in degrees from the pointer pair's starting angle.
`Gesture.Pinch()` and `Gesture.Rotate()` can both be registered on the same
ref to read scale and rotation from one two-finger gesture.

```jsx
useGesture(
  ref,
  Gesture.Rotate().onChange(({ rotation }) => setAngle(rotation))
);
```

Config: `.enabled(bool)`, `.threshold(degrees)` (minimum rotation before
recognized as active, default `2`).
Event shape: `{ phase, rotation, velocity, center, event, target, cancel }`.

<CodeExample
  demo={Demo_gesture_useGesture_RotateExample}
  code={Code_gesture_useGesture_RotateExample}
/>

## Array mode

Passing an array of refs registers each one individually, diffed by ref
identity (not array length). Reordering or adding/removing refs doesn't
tear down unrelated registrations. Every emitted event gets `index` merged
in:

```jsx
useGesture(
  cardRefs,
  (index) => Gesture.Pan().onChange(({ movement, index }) => setX(index, movement.x))
);
```
