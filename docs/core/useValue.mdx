---
sidebar_position: 1
id: use-value
title: useValue
---

import PreviewWithCode from '@site/src/components/Blocks/PreviewWithCode';
import AnimateOffset from '@site/src/components/UseValue/primitive/AnimateOffset';
import AnimateColor from '@site/src/components/UseValue/primitive/AnimateColor';
import AnimateOffsetCode from '!!raw-loader!@site/src/components/UseValue/primitive/AnimateOffset';
import AnimateColorCode from '!!raw-loader!@site/src/components/UseValue/primitive/AnimateColor';

The `useValue()` function is a powerful hook designed to manage and animate values within your React components without triggering unnecessary re-renders. It behaves similarly to a [ref](https://react.dev/reference/react/useRef), holding the current animation state internally.

This hook returns a tuple: `[value, setValue]`.
- **value**: The current animated state, which you can directly apply to JSX styles or component props.
- **setValue**: A function to update the `value`, which can trigger a smooth animation when used with motion modifiers.

## Basic Usage

The primary use case for `useValue()` is to animate properties. For instance, to animate an element's offset:

<PreviewWithCode title="App.tsx" preview={<AnimateOffset />} code={AnimateOffsetCode} language="jsx"/>

:::info Immediate vs. Animated Updates
When you assign a raw value (e.g., `setOffset(200)`), the property changes **immediately** without animation. To achieve a smooth transition, you must wrap the target value in a [animation modifier](animation-modifiers-overview) like ([withTiming](animation-modifiers-withTiming) or [withSpring](animation-modifiers-withSpring)).
:::

***

## Usage 
### 1. Primitive Values (Number & String)
For primitive data types such as numbers or strings, `useValue` manages a single animated value directly.
##### Syntax
```jsx
const [value, setValue] = useValue(initialValue: number | string);
```
<details>
  <summary>View example</summary>
  <PreviewWithCode title="App.tsx" preview={<AnimateOffset />} code={AnimateOffsetCode} language="jsx"/>
</details>

### 2. Array of Primitives
You can also animate an array of values. The hook stores the array and you update it similarly:
##### Syntax
```jsx
const [arr, setArr] = useValue(initialArray: number[]);
```
<details>
  <summary>View example</summary>
  <PreviewWithCode title="App.tsx" preview={<AnimateOffset />} code={AnimateOffsetCode} language="jsx"/>
  </details>

### 3. Object of Primitives
You can also initialize useValue with an object whose fields are numbers or strings. The hook returns a matching object
##### Syntax
```jsx
const [obj, setObj] = useValue(initialObject: { [key: string]: number | string });
```
<details>
  <summary>View example</summary>
  <PreviewWithCode title="App.tsx" preview={<AnimateOffset />} code={AnimateOffsetCode} language="jsx"/>
  </details>

{/* ##### Syntax
```jsx
const [value, setValue] = useValue(initialValue: number | string);
```

#### Example 1: Animating a Number (`useValue(0)`)

This example demonstrates how to animate a numerical offset.

<PreviewWithCode title="App.tsx" preview={<AnimateOffset />} code={AnimateOffsetCode} language="jsx"/>


### Example: Animating a Color (`useValue('red')`)

This example shows how `useValue` can handle string-based animations, specifically for colors.

<PreviewWithCode title="App.tsx" preview={<AnimateColor />} code={AnimateColorCode} language="jsx"/> */}
