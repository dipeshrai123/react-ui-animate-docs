---
sidebar_position: 6
id: withSequence
title: withSequence
---

The `withSequence` modifier allows you to chain multiple animations together, executing them one after another in the order they are defined. This is useful for creating complex, multi-step animations where each step needs to be completed before the next begins.

## Syntax
```jsx
withSequence(...animations: Animation[]) => Descriptor
```

### Parameters
- **...animations**: A series of animation descriptors ([withSpring](./withSpring), [withTiming](./withTiming), [withDecay](./withDecay), etc.) that will be executed sequentially.

##  Example
import BasicExample from "@site/src/examples/strings/v4.x.x/modifier/withSequence/BasicExample"

import SandPack from "@site/src/components/SandPack"

<SandPack version={{
   reactAnimate: '5.0.0-rc.9'
}}
files={{
  "App.tsx": BasicExample
}}
/>