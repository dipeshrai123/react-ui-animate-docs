---
sidebar_position: 1
id: hover-animations
title: Hover Animations
---

import CodeExample from '@site/src/components/CodeExample';
import Code_interactive_HoverExample from '!!raw-loader!@site/src/examples/6.0.0/interactive/HoverExample.tsx';
import Demo_interactive_HoverExample from '@site/src/examples/6.0.0/interactive/HoverExample';

The `hover` prop animates on pointer enter/leave.

## Usage

```jsx
import { animate, withSpring, recipes } from 'react-ui-animate';

// highlight-start
/* hover animates on pointer enter / leave */
<animate.button hover={{ scale: withSpring(1.1) }}>Hover me</animate.button>
// highlight-end

// highlight-start
/* Or pass a ready-made recipe */
<animate.button hover={recipes.hoverScale}>Preset</animate.button>
// highlight-end
```

<CodeExample
  demo={Demo_interactive_HoverExample}
  code={Code_interactive_HoverExample}
/>

## Combining with other props

```jsx
// highlight-start
/* hover + press compose — each trigger owns its target */
<animate.button
  hover={{ scale: withSpring(1.1) }}
  press={{ scale: withSpring(0.95) }}
>
  Interactive Button
</animate.button>
// highlight-end
```

:::note
`hover` only fires from real pointer enter/leave. On touch devices, prefer
[press](./press-animations) instead: there is no lasting hover state.
:::

## Next steps

- [Press Animations](./press-animations): pairs naturally with `hover`.
- [Focus Animations](./focus-animations): for keyboard navigation.
